export default function BottomBar () {
    return (
        <div class="bottom-bar">
            <button class="bottom-bar-button" onclick="confirm()">
                <p class="font-weight-400">Selecione os 3 itens<br />para fechar o pedido</p>
            </button>
        </div>
    );
}